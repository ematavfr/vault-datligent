# Configuration Vault pour l'infrastructure Datligent
export VAULT_ADDR="http://localhost:8200"
export VAULT_TOKEN="datligent-root-token"

# Pour charger cette configuration:
# source .env.vault

echo "✅ Configuration Vault Datligent chargée"
echo "📍 VAULT_ADDR: $VAULT_ADDR"
echo "🔑 Token configuré"